# Company Secretarial (CoSec) track documentation

Track-specific documentation for Oscar's Company Secretarial paralegal
capability. The track is about to begin; contents will accumulate as
Sprint C1 and beyond proceed.

## Layout (expected, empty for now)

- `reference/` — CoSec-specific reference material.
- `research/` — exploratory research tied to specific CoSec sprints.

## Where other material lives

- **Sprint log.** CoSec sprints are logged chronologically in the
  repo-root `SPRINT_LOG.md` with `[CoSec]` heading tags. The
  `PROJECT.md` Sprint Index carries one-line summaries.
- **Cross-track SDK references.** `docs/reference/`.
- **Architecture Decision Records.** Single numbering sequence across
  tracks — CoSec ADRs carry the `[CoSec]` tag in the title.
  `docs/adr/`.
- **Track-shared architectural principles.** `PROJECT.md`
  (track-specific principles live here instead from M1 onwards).
