# PROJECT.md — Oscar Enterprise

> This file describes WHAT Oscar is. See CLAUDE.md for HOW to write the code.
> Read both files before writing any code.
>
> This is a goal document. It describes the destination, not the path.
> Build decisions emerge stage by stage and are captured in ADRs as they are made.

## What Oscar Is

Oscar is an AI agent that automates large parts of in-house legal work — commercial transactions, company secretarial, privacy compliance, and more. Oscar is delivered as a service by law firms to their clients, with a dedicated VPS per client, fully isolated, governed by NVIDIA OpenShell. Each client's Oscar learns the client's preferences and house positions over time and becomes increasingly tailored to how that client wants legal work done.

Oscar is multi-capability. Capabilities are added in stages. They share a common foundation: the same agent harness, the same governance discipline, the same memory and audit principles. New capabilities slot in alongside existing ones rather than as separate products.

Oscar's first capability is **contract redlining**. Oscar reads contracts the way a commercial lawyer would — clause by clause, against the client's playbook, with an eye to commercial substance — and returns marked-up versions with native track changes. Subsequent capabilities are added as the work matures.

---

## Tracks

Oscar's capabilities are developed in parallel tracks. Each track has its own sprint series and its own directory for track-specific code (`src/{track}/`) and documentation (`docs/{track}/`). Current tracks:

- **Redline** — contract redlining (Phase 1 capability). 10-series sprints. See `docs/redline/`.
- **CoSec** — Company Secretarial paralegal. C-series sprints, beginning with C1. See `docs/cosec/`.

Cross-track infrastructure sprints use the **M-series** (Sprint M1 established this structure). Tracks share a single `main` branch, a single ADR numbering sequence (`docs/adr/`), a single `SPRINT_LOG.md`, and a single `TODO.md`, with track tags distinguishing ownership. See `CLAUDE.md § Multi-Track Discipline` for the rules.

Track-specific architectural principles live in `docs/{track}/`, not here. This file carries only cross-track substance.

---

## One VPS, One Client, No Multi-Tenancy

Each client gets their own VPS. There is no multi-tenant architecture. No row-level security. No tenant_id columns. One database, one Oscar instance, one client.

This means:

- No tenant isolation logic in the application code
- No tenant context middleware
- No RLS policies
- Simple, direct database queries
- Complete isolation by infrastructure, not by code

Client-specific configuration lives in `oscar_config.yaml`, not in a tenants table.

---

## What Oscar Does

Oscar is a working legal team in software. It does the kinds of things a junior to mid-level in-house lawyer would do — read documents, propose changes, ask questions, generate outputs, learn what the client wants — across a growing range of capability areas.

Oscar communicates in plain English. No legalese unless the output is itself a legal document. Oscar leads with materiality (what matters, why), gives opinions where asked, and flags uncertainty rather than papering over it.

### Human Authority

Humans are always the final authority on Oscar's output. Oscar proposes, humans confirm. Oscar's confidence in its own outputs is always provisional until a human signs off.

### Audit

Every action Oscar takes is recorded — what changed, from what source, when, by whom. Append-only. Oscar's behaviour is inspectable end to end.

### Learning

Oscar learns from its clients over time. Preferences, house positions, drafting styles, walk-away thresholds — these accumulate through the natural course of work. Oscar does not autonomously rewrite its own knowledge; updates flow through human approval. The learning loop is the substantive differentiation: each client's Oscar becomes that client's Oscar.

---

## What Oscar Does NOT Do

- Provide legal advice on its own authority — Oscar is a tool used by solicitors
- Delete data (soft delete only)
- Modify its own code, prompts, or knowledge autonomously
- Send communications outside configured channels
- Make external API calls to anything not declared in its sandbox network policy

---

## Technology Stack

| Layer | Technology |
|-------|-----------|
| Runtime | Python 3.13 |
| Sandbox | NVIDIA OpenShell |
| Agent harness | Deep Agents on LangGraph |
| Database | PostgreSQL |
| LLM (runtime) | Model-agnostic via dependency injection |
| LLM (build) | Claude Code |
| Observability | LangSmith + OpenTelemetry |

Specific libraries within these layers (memory tools, channel adapters, document processing engines, etc.) are chosen per capability stage and captured in ADRs.

### LLM Policy

Oscar's runtime LLM is model-agnostic by design. Model choice is a dependency injection at startup, not hardcoded in agent code. Clients with sovereignty concerns can configure their own provider.

The build-time agents (Claude Code) are exempt from this policy — they are tools for humans, not part of Oscar's runtime.

### Model Allocation

Model choice is dependency injection, not hardcoded. The architectural principle:

- The orchestrator (General Counsel) uses the strongest available frontier reasoning model. Today that might be GPT-5.4 or Opus 4.7; tomorrow it will be whatever has superseded them. Orchestration is reasoning-heavy and relatively low-volume — spend the tokens here.
- Specialist agents that do the substantive work run on capable-but-cheaper models. In DEV that is MiniMax. In PROD it might be Sonnet or similar. The right choice per specialist depends on per-agent evaluation — some will need more capable models, others won't.
- No agent hardcodes its own model. Allocation lives in configuration and is injected at startup.

---

## Sandbox

Oscar's runtime runs inside an OpenShell sandbox. This is foundational, not optional. Network policy is default-deny. Every external endpoint Oscar talks to is explicitly allowed in policy YAML, with method-level enforcement where it matters. Policy YAML is version-controlled in the repo.

---

## Capability Stages

**Phase 1 (current): Contract redlining.** Oscar reads commercial contracts and returns redlined versions reflecting the client's playbook. Edits are returned as native Word track changes. The playbook learns from client conversations.

**Future phases** will be specified in this file as they are approached. Likely areas:

- Company secretarial
- Privacy compliance
- Transactional support (M&A, due diligence)
- Further areas as the in-house legal capability surface expands

The order in which future phases are tackled depends on what we learn from earlier ones, on commercial priorities, and on what client demand looks like as Oscar reaches market.

---

## Files in Project Root

| File | Purpose |
|------|---------|
| README.md | Brief project description |
| PROJECT.md | This file — what Oscar is; includes the Sprint Index |
| SPRINT_LOG.md | Append-only detailed record of every sprint (paired with the Sprint Index in PROJECT.md) |
| CLAUDE.md | Coding standards — governs how Oscar's code is written |
| oscar_config.yaml | Runtime configuration |
| .env.example | Template for required environment variables |
| policies/ | OpenShell policy YAMLs |
| docs/adr/ | Architecture Decision Records |
| src/ | Application source |
| tests/ | Test source |

Deployment-time exclusion of non-runtime files (PROJECT.md, CLAUDE.md, docs/adr/) is a build/package-time concern — handled via a deployignore or equivalent when SIT is stood up — not a reason to keep files outside git. Git is the durability mechanism; what ships to SIT is a later, separate concern.

---

## Sprint Index

> One-line summaries of each sprint, chronological. The full append-only record — goals, findings, surprises, ADRs, carry-forwards — lives in `SPRINT_LOG.md`. Read the most recent entry there to know where work picks up; use this index to identify which older entries are worth reading for the task at hand.

- **Sprint 0 [Redline] (2026-04-18) — Establish workflow.** Sprint log bootstrapped in PROJECT.md; sandbox ready for repo clone. No technical artefacts.
- **Sprint 1 [Redline] (2026-04-18) — Install LangGraph core.** `langgraph 1.1.8` installed into `/sandbox/.venv`; `pip install langgraph` pulls `langchain-core`, `langgraph-prebuilt`, `langgraph-sdk`, `langsmith`, and the in-memory `langgraph-checkpoint` as hard deps. Version lives at `langgraph.version.__version__` — langgraph is a PEP 420 namespace package with no top-level `__version__`. PyPI egress was already open; no policy widening needed.
- **Sprint 2 [Redline] (2026-04-18) — Minimal LangGraph runs.** Smallest 2-node graph (`START → append_hello → append_world → END`) runs end-to-end with a TypedDict state. `docs.langchain.com` is policy-blocked, so worked from installed source per "code outranks docs." Node returns overwrite state by default — reducers are needed for accumulation. `StateGraph(config_schema=...)` is soft-deprecated in 1.1.8 in favour of `context_schema`.
- **Sprint 3 [Redline] (2026-04-18) — MiniMax LLM round-trip.** `MiniMax-M2.7` round-trips through a 2-node graph via raw `httpx`; DI seam built at `src/llm/` with `_FACTORIES` dispatch (adding a provider is one dict entry + one factory). `langchain-community.MiniMaxChat` rejected as stale (issue #29278, wrong host/default). MiniMax's OpenAI-compat endpoint returns chain-of-thought inline in `<think>...</think>` tags (deferred to Sprint 8). ADR 008.
- **Sprint 4 [Redline] (2026-04-18) — OpenRouter as a second provider.** OpenRouter plugs into the DI seam with zero graph-code changes — validates ADR 008's "provider is one branch, not a rewrite" claim. `openai/gpt-5.4` returns clean text with no `<think>` wrapper, so MiniMax's reasoning-trace behaviour is provider-specific, not OpenAI-compat-general. OpenRouter is a US broker fronting many upstreams — sovereignty decisions move from provider-choice to model-slug-choice.
- **Sprint 6 [Redline] (2026-04-18) — Deep Agents runs end-to-end.** `deepagents 0.5.3` runs a smallest-meaningful agent with a `slugify` tool + filesystem + planning against `openai/gpt-5.4` on OpenRouter. Parallel `BaseChatModel` seam added at `src/llm/chat_model.py` because the Sprint-3 string seam can't do tool-calling. Every Deep Agent gets a latent `general-purpose` subagent — `SubAgentMiddleware` is unconditional, so single-agent postures are prompt-level only. `requirements.txt` frozen at 54 pinned packages. ADR 009.
- **Sprint 7 [Redline] (2026-04-18) — General Counsel + Head of Commercial routing scaffolding.** Two-role GC-over-HOC org chart routes correctly on two test invocations (NDA delegates to HOC via `task`; Companies House declined without delegation). MiniMax factory added to chat-model seam via `init_chat_model("openai:MiniMax-M2.7", base_url=...)`. Per-role env-var triples (`OSCAR_LLM_GENERAL_COUNSEL_*`, `OSCAR_LLM_HEAD_OF_COMMERCIAL_*`) are the DI mechanism. `tools=[]` on `SubAgent` inherits default middleware tools — "toolless" is prompt-level, not framework-level. `docs/secrets.md` seeded. ADRs 010, 011.
- **Sprint 8 [Redline] (2026-04-18) — Resolve MiniMax `<think>` pollution flagged in Sprint 3.** One-line fix: `extra_body={"reasoning_split": True}` in `_minimax_factory`. MiniMax splits reasoning into a separate `reasoning_details` field, which LangChain's `_convert_dict_to_message` drops by design — `AIMessage.content` arrives clean. Amends ADR 011's "`reasoning_split` not reachable" claim: provider-specific *request* parameters ARE reachable via `extra_body`; only non-standard *response* fields are dropped. Lesson: prefer the native knob before reaching for post-processing. ADR 012.
- **Sprint 9 [Redline] (2026-04-18) — Accept/reject reasoner (first functional specialist under Head of Commercial).** Three-level delegation GC → HOC → `accept-reject-reasoner` works end-to-end on three test invocations (accept unchanged, reject Delaware, counter Scotland). `with_structured_output(method="json_schema")` fails against MiniMax's OpenAI-compat shim — `AutoStrategy` auto-falls-back to `ToolStrategy` which works reliably. `CompiledSubAgent` is the documented nesting pattern (`SubAgent` TypedDict has no `subagents` field). MiniMax tool-call discipline was ~67% on first prompt, 100% with an explicit output-discipline preamble. `structured_response` doesn't propagate up through the `task` tool. ADRs 013, 014, 015, 016.
- **Sprint 10A [Redline] (2026-04-19) — Adeu integration research (plan only, no code).** `docs/research/sprint-10-adeu-integration.md` reads Adeu 1.1.0 and Claude-Plugin-MCP from source. Proposes SDK integration (not CLI or MCP server), a new `redline-specialist` under HOC via `CompiledSubAgent`, a three-sprint split (10B substrate / 10C wiring / 10D verification). Prior-art discipline distilled to three lawyer-shape rules. Adeu's `RejectChange` only cancels own prior edits; counterparty-text deletion is only reachable via over-broad `ModifyText`. 10 risks surfaced.
- **Sprint 10B [Redline] (2026-04-19) — Install Adeu 1.1.0 and prove SDK works mechanically (substrate only).** `adeu==1.1.0` installed into `/sandbox/.venv` (59 new transitives; `requirements.txt` 59→119). `src/experiments/sprint-10b-adeu-bare-bones/run.py` generates a synthetic `.docx`, applies three hardcoded `ModifyText` edits via `RedlineEngine.process_batch`, and verifies structurally correct OOXML: each modification produces `w:del`+`w:ins` with `w:author="Oscar"` and original text preserved in `<w:delText>`; insertion produces a single `w:ins`. Pure insertion is NOT a first-class SDK primitive — 10A's "empty target_text" recipe is rejected by the engine; the supported idiom is prefix-match (`new_text.startswith(target_text)` is synthesised as INSERTION internally). `CommentsManager` eagerly creates four comments-related parts even when unused. Each modification emits two tracked change IDs, not one. Adeu ready for 10C agent integration.
- **Sprint 10C [Redline] (2026-04-19) — Adeu API reference, test battery, idioms, and lawyer-shape criteria (research only).** Four artefacts committed: `docs/reference/adeu-api-reference.md` (~750 lines, exhaustive operation inventory covering every `adeu.__all__` symbol + `adeu.sanitize` + `BatchValidationError`), `src/experiments/sprint-10c-adeu-reference/` (5 themed suites + harness + runner; 82/82 passing on adeu==1.1.0), `docs/reference/adeu-idioms.md` (intent-organised usage guide — phrased to be quoted into Sprint 10D's prompt), `docs/reference/adeu-lawyer-shape-criteria.md` (DRAFT success criteria per NDA transformation; awaits Arturs's review). Ten new findings not in 10A/10B — notably: fuzzy regex matches `\n\n` as `\s+` (cross-paragraph targets succeed); `trim_common_context` narrows full-sentence mods to word-level diff (does 80% of lawyer-shape work automatically); comments on pure deletions silently dropped; non-owning author CAN reject by id (qualifies 10A #6 — audit-trail protection must live above Adeu); `ReplyComment` on missing parent silently adds stray comment; `comment` field on Accept/RejectChange is vestigial. 10B follow-up (b) solved: structlog WARNING-routing pattern reusable. Four questions flagged for Arturs's human decision before 10D begins. No ADRs; no requirements.txt changes; no egress changes.
- **Sprint 10D [Redline] (2026-04-19) — First end-to-end agent-driven redline: litigation → arbitration on a synthetic NDA.** GC → HOC → `redline-specialist` (new, MiniMax-M2.7) chain produces `src/experiments/sprint-10d/nda-output.docx` — a valid 21-part `.docx` with `w:ins × 2 + w:del × 2` authored by "Oscar". All three mechanical checks pass (exists, valid zip, parseable `word/document.xml`). Tool surface: two tools — `modify_text` (thin wrapper) and `insert_text` (facilitator, ADR 018). `add_comment` deliberately omitted (Adeu 1.1.0 SDK has no clean standalone-comment primitive; manufacturing one would be a wrapper per ADR 018). Prompt iteration 1 failed — HOC fabricated a "file not found" response without delegating; fixed in iteration 2 by adding a "no filesystem access, cannot claim files are missing" discipline preamble. Iteration 2 passed mechanical checks but surfaced two findings for 10E: (a) MiniMax specialist over-tool-called (double modify_text, nesting an edit inside its own prior `w:ins` and clearing the original text from the audit trail); (b) HOC paraphrased specialist output with hallucinated "file didn't exist, created a representative NDA instead" narration — the transformation was on the real NDA. Clean view (Accept-All simulated) renders correctly; raw view is muddled. Handoff: Arturs opens `nda-output.docx` in Word; Sprint 10E iterates. ADRs 017 (`.docx` file flow via filesystem), 018 (facilitator-vs-wrapper boundary). New env-var triple for redline specialist. No new deps, no policy widening.
- **Sprint 10E [Redline] (2026-04-19) — Import surgical-span discipline from Claude-Plugin-MCP; re-run 10D's transformation with lawyer-shape output.** One iteration. Specialist system prompt rewritten to carry the decomposition work Adeu's `trim_common_context` can't do (litigation↔arbitration sentences share no prefix/suffix, so whole-sentence targets can't narrow). The rewrite adapts Claude-Plugin-MCP `SKILL.md` Step D1 (lines 648-689) — distilled for MiniMax per Sprint 9 — and hands the specialist an explicit two-call decomposition: CALL 1 `modify_text` swaps the 11-word forum phrase ("the exclusive jurisdiction of the courts of England and Wales" → "binding arbitration under the LCIA Rules"), CALL 2 `insert_text` appends the 30-word seat/language/arbitrator/finality sentence after the closing full stop. MiniMax executed both calls byte-identically to the prompt on first run. Output `src/experiments/sprint-10e/nda-output.docx`: `w:ins × 2 + w:del × 1`; `w:del`=10 words, `w:ins`=6 words, `w:ins`=30 words (suspicious-but-substantive new sentence, acceptable); no nested-delete, no duplicate-ins, litigation phrase preserved in `w:delText`. `verify_output` extended with four lawyer-shape warning checks operationalising the brief's self-verification criteria. Tool implementations added a `_TOOL_CALL_CAPTURE` + `tool-calls.jsonl` log because Deep Agents' `task` tool hides subagent tool calls from the outer trace (confirmed empirically — new surprise). **Scope boundary surfaced for future readers: 10E tests EXECUTION of surgical-span discipline (prompt supplies the exact spans), not IDENTIFICATION — a future sprint tests whether MiniMax can produce the decomposition itself.** No new ADRs, no new dependencies, no policy widening, no env-var changes.
- **Sprint 10F [Redline] (2026-04-20) — Identification test: can MiniMax find the surgical spans itself, or does it need 10E's hand-wired decomposition?** Partial result (feature-branch only — not merged to main per brief's unsuccessful-sprint rule). Two iterations. Specialist prompt stripped of 10E's explicit CALL 1 / CALL 2 decomposition; left only the general surgical-span rule + the target arbitration shape (five named elements) + shape guidance ("one wide call wrong; many tiny fragments wrong; a small handful of narrow edits"). Iteration 1 reproduced Sprint 10D iteration-1's hallucinate-missing-file failure at the specialist layer — with 10E's crutch removed, MiniMax had no definite instruction to anchor on and defaulted to "the directory and file do not exist"; zero tool calls. Iteration 2 added an OPERATING DISCIPLINE section forbidding missing-file claims (same-shape fix as 10D HOC iteration 2, now needed on the specialist because it is doing the open-ended reasoning). After the fix: specialist made 2 tool calls but one was a degenerate no-op (`target_text == new_text`); the other bundled ALL the substantive change (forum swap + seat + rules + arbitrator + language + finality) into one wide `modify_text`. OOXML: `w:ins=1 (33 words, suspicious), w:del=1 (12 words, clean)`; audit trail preserved; clean-view §9 contains all five required arbitration elements and reads coherently. **Finding: MiniMax identifies at the clause level (finds the forum sentence, recalls all five arbitration elements correctly) but cannot autonomously decompose into the 10E-style narrow multi-call pattern — it bundles instead.** Research note: Claude-Plugin-MCP's SKILL.md does NOT explicitly teach span-level identification (Step B is clause-level, Step D1 is span-sizing not span-finding); its reliance on Opus's judgement is exactly the capability being tested. Sprint 10G proposed as fallback: same 10F prompt, specialist swapped to `openai/gpt-5.4` via the ADR 008 DI seam (one env-var triple flip); three-way diagnostic (GPT-5.4 produces 10E shape → frontier-model capability finding; both fail → 10H ports the code-level word-diff pipeline; in-between → graduated-tier design space). New standing carry-forward: OPERATING DISCIPLINE preamble is now required on any MiniMax specialist doing open-ended reasoning. No new ADRs, no new dependencies, no policy widening, no env-var changes.
- **Sprint 10G [Redline] (2026-04-20) — Plan-before-act test: does a "plan first" prompt section close 10F's decomposition gap on MiniMax?** Outcome C — feature-branch only per brief's unsuccessful-sprint rule; sprint log entry on main. One prompt-section addition (`PLAN BEFORE YOU ACT` — 160 words, numbered plan with four fields per edit, "I will make N edits." opening, no placeholders, execute-the-plan-in-order). MiniMax did NOT produce the structured plan format — its response was free-form `<think>`-wrapped reasoning. That reasoning explicitly committed to "make a single modification to replace it with arbitration language" — bundling at the planning stage, not at execution time. Execution matched: one `modify_text`, `w:del`=29 words (suspicious), `w:ins`=41 words (suspicious), all five arbitration elements present, clean-view §9 coherent. Wider than 10F on both sides of the redline. **Finding: the plan-first instruction did not shift MiniMax's decomposition behaviour. The reasoning itself converges on bundling — decomposition is a capability ceiling, not a planning absence. 10F's hypothesis is falsified.** Plan-capture required a minimal observability fix (reasoning_split=False local override for the specialist only) because LangChain's `_convert_dict_to_message` drops MiniMax's `reasoning_content` / `reasoning_details` (ADR 012's production-clean default) — verified via direct httpx probe and LLMResult field audit. Five new surprises — notably: (1) MiniMax's planning-stage reasoning itself converges on one-edit bundles, not just its tool-calls; (2) MiniMax ignores plan-FORMAT directives (free-form reasoning produced in place of the structured "I will make N edits." template); (3) the hallucinate-missing-file confusion surfaces at the REASONING layer now that reasoning is visible, and is overcome internally — OPERATING DISCIPLINE works at reply-level but not reasoning-level; (4) default Deep Agents filesystem tools (`ls`, `read_file`) contaminate restricted-tool specialists — OPERATING DISCIPLINE's "no filesystem access" is prompt-level fiction; (5) MiniMax reasoning is reachable only via `reasoning_split=False` local override or a custom LangChain subclass (not via any stock callback/metadata channel). Architectural-judgement section recommends 10H as MODEL SWAP (GPT-5.4 on 10F's prompt — not 10G's; we want raw decomposition capability, not prompt-forced planning) before any planner/executor split; if GPT-5.4 also bundles, the compelling path is LLM planner + CODE executor (Claude-Plugin-MCP word-diff pattern), which is structurally different from a two-LLM split. No new ADRs, no new dependencies, no policy widenings, no env-var changes.
- **Sprint 10H [Redline] (2026-04-20) — Planner/executor split (Shape A): GPT-5.4 planner + MiniMax executor under Head of Commercial.** Outcome C with a confounding architectural finding — feature-branch only; sprint log entry on main. Primary: GC → HOC → `redline-planner` (new, GPT-5.4 via OpenRouter) → HOC → `redline-executor` (rename of `redline-specialist`, MiniMax-M2.7) → HOC → GC. Plan data contract: fenced ```json block with one edit object per entry (`tool` discriminator, `target_text`/`anchor_text`, `new_text`, `reason`), parsed by the executor with dispatch-on-tool-first validation. Control: executor alone, handed 10E's hand-decided spans in the 10H plan format — produced `w:ins=2, w:del=1`, clean 10E-shape output, confirming executor's pure-execution discipline is intact. Primary produced `w:ins=0, w:del=0`: the planner was invoked twice (plan-1 had a literal `[existing litigation/jurisdiction sentence - replace entirely]` placeholder in `target_text`, violating its own no-placeholders rule; plan-2 hallucinated `"The Parties irrevocably submit ..."` — stylistically plausible but does not byte-match the NDA, which reads `"The parties submit ... for the resolution of all disputes ..."`). Both plans converge on the single-edit shape. The executor refused plan-1 without calling a tool; called `modify_text` once on plan-2; Adeu rejected (target unmatched); executor stopped per discipline. HOC then fabricated to GC: *"the redline-executor returned an explicit error ... a subsequent filesystem search confirmed the file ... does not exist — only `plan-error.json`"* (no filesystem search happened; no `plan-error.json` exists). **Finding: the primary failure is not (as the brief anticipated) GPT-5.4 failing to decompose, but MiniMax HOC dropping the §9 clause text when relaying to the planner.** The plan's risk-ledger #5 mitigation (*"HOC relays verbatim"*) failed empirically — HOC paraphrased out the clause text despite an explicit prompt-level rule to include it verbatim. Plan-to-tool-call mapping was faithful at the one step that ran (plan-2 → byte-identical executor call); faithfulness at the HOC→planner leg is what broke. Additional finding: HOC's fabricated filesystem-narrative is the fourth concrete manifestation of the 10D Surprise 3 paraphrasing hazard — promoted to must-fix-before-Shape-A level. Sprint 10I proposal: route around HOC for the planner's input leg (inject clause text into planner's system prompt at build time, OR have Python orchestrate planner→executor rather than HOC), then re-test to finally answer the GPT-5.4-decomposition question. The decomposition-capability question remains untested because the test was confounded. No new ADRs (019-021 queued to land only if Shape A produces lawyer-shape output — it didn't on this run). New env-var triples on the feature branch only (`OSCAR_LLM_REDLINE_PLANNER_*`, `OSCAR_LLM_REDLINE_EXECUTOR_*`); old `OSCAR_LLM_REDLINE_SPECIALIST_*` remains on main until 10I confirms the rename. Control-run success and the validated plan-data contract are the salvageable parts.
- **Sprint 10I [Redline] (2026-04-21) — Executioner-framing capability test: can clause-scoped single-agent framing unlock decomposition that document-scoped framing couldn't?** Feature-branch only per unsuccessful-sprint rule; sprint log entry on main. Pivot from TODO item 42's proposed 10I (HOC-bypass re-run of Shape A) to a capability-first test: before designing planner + multiple executioners architecture, answer whether MiniMax can function as a clause-level executioner at all. Single MiniMax agent, direct `agent.invoke()`, no GC/HOC/subagents, executioner-framed prompt (pre-decided goal, scope §9 only, count-and-shape DECOMPOSITION DISCIPLINE guidance replacing 10E's hand-wired spans). **MiniMax primary: Outcome D (not A/B/C)** — zero substantive tool calls. MiniMax first called `read_file` on the input path (Deep Agents' `FilesystemMiddleware` injects `ls`/`read_file`/`glob`/etc. unconditionally at top level; virtual `StateBackend` doesn't see Adeu's real filesystem), got "not found", looped `ls` ×3 and `glob`, concluded "File not found. Execution blocked." This is 10F iter-1's hallucinate-missing-file failure mode reproduced — TODO item 6's *"no filesystem access is prompt-level fiction"* warning made concrete and blocking. Prior sprints' sub-agent shape hid this because the sub-agent HumanMessage is `task`-synthesised with no explicit path reference. **Sonnet reference sub-experiment (same Deep Agents, same prompt, same invocation, Sonnet 4.6 via OpenRouter): Outcome C** — ignored framework filesystem tools cleanly, went straight to `modify_text` on first turn, but bundled the entire transformation into ONE wide edit (`w:ins`=71 words OVER-BROAD, `w:del`=29 words SUSPICIOUS, all five arbitration elements in one monolithic insertion). Wider bundling than any prior MiniMax run under any framing. **Joint finding: the framework filesystem-tool leak is MiniMax-specific, but the decomposition ceiling under executioner framing with count-and-shape guidance (no worked example, no hand-wired spans) is NOT model-tier — Sonnet bundles harder than MiniMax under 10F/10G's document-framing. The prompt-framing ceiling is the ceiling; executioner framing alone doesn't unlock surgical-span decomposition.** 10J shifts from "all-MiniMax executioner architecture" (no longer viable) to Shape B (Claude-Plugin-MCP word-diff deterministic executor port under an LLM planner). TODO.md updates: item 6 promoted to must-fix for top-level MiniMax; item 9 amended (all-MiniMax and single-specialist-on-frontier both ruled out; Shape B trigger moves from contingent to immediate-10J); item 42 marked superseded. No new ADRs, no new dependencies, no policy widenings, no `.env.example` changes. Sonnet triple set at shell level for the diagnostic run only.
- **Sprint 10J [Redline] (2026-04-21) — Deterministic edit decomposition pipeline: MiniMax drafts + Python word-diffs + Adeu applies.** Feature-branch only per unsuccessful-sprint rule; sprint log entry on main. Three-stage shape — Stage 1 (single `chat_model.invoke` to MiniMax with minimal drafting prompt, no narrow-edit / decomposition language, no conservation-guidance sentence either; JSON output `{current_text, replacement_text}` with echo-integrity check + Unicode normalisation), Stage 2 (pure-Python word-diff via `diff-match-patch` v20241021 with Unicode token encoding + `diff_cleanupSemanticLossless` + single-pass block-grouping absorbing short EQUAL gaps < 2 content tokens + ±8-word uniqueness widening + cross-edit coupling check), Stage 3 (direct `RedlineEngine.process_batch`, no `@tool` wrappers, no agent loop). No Deep Agents usage anywhere. **Outcome B — diagnosable single-stage failure at Stage 1.** Stage 1 produced clean JSON with verbatim `current_text` echo (zero character divergence) and a coherent arbitration `replacement_text`. Stage 2's word-diff correctly identified the change as one logical modification block (71 raw ops collapsing to 1 modify edit after block-grouping). Stage 3 applied atomically — `edits_applied=1, edits_skipped=0`, audit trail clean, five arbitration elements present, governing-law sentence intact. **But the single edit is lawyer-shape-fail**: `w:ins`=54 words (OVER-BROAD, >50), `w:del`=29 words (SUSPICIOUS, >20). The failure locates at Stage 1 — MiniMax's natural drafting style without conservation guidance is **wholesale sentence-level rewrite** (original and replacement share only incidental function words), so the diff has only one logical block to emit. Substrate (Stages 2+3) works correctly; the drafter's output shape doesn't grain into narrow edits. **Decomposition ceiling re-characterised:** it's not decomposition per se, it's the natural output shape of a model asked to produce target clause text — regardless of whether framed as drafting or editing. The Plan-agent-advised decision (confirmed with Arturs at plan time) to drop conservation language gave the cleanest possible negative result: no prompt steering, pure substrate behaviour. 10K primary: re-run Stage 1 with a conservation-discipline sentence ("preserve original wording wherever it still reads correctly — change only what the transformation requires"), keep Stages 2+3 identical; if MiniMax ignores it (as 10G's plan-first discipline was ignored), the substrate doesn't rescue us from model shape at drafting stage either, and 10L reconsiders. Echo-integrity PASS is a new data point against TODO-8's HOC paraphrase hazard — the tier can relay verbatim under structured-output discipline. No new ADRs, no new dependencies (`diff-match-patch==20241021` was already pinned), no policy widenings, no `.env.example` changes.
- **Sprint 10K [Redline] (2026-04-21) — Port Claude-Plugin-MCP's first-pass pattern to MiniMax (faithful port test).** Feature-branch only per unsuccessful-sprint rule; sprint log entry on main. Phase 1 research note on main at `docs/redline/research/sprint-10k-claude-plugin-mcp-port.md` (CPM source trace + 10J↔CPM divergence analysis + CPM↔Adeu version-gap analysis per new CLAUDE.md §"Cross-Version Porting Research"). Finding A from version check: Adeu v0.9.0 renamed `DocumentEdit` → `ModifyText` (unified `DocumentChange` API); field names unchanged; one-line Python translation; LLM-visible contract identical. **10J's pipeline was not actually a CPM port** — it was LLM-drafts-prose + document-level-diff, while CPM is LLM-produces-edit-list + per-edit-diff. 10K replaces the 10J pipeline with the faithful pattern: single LLM call; system prompt assembles PERSONA.md (29 lines) + AUTHORITY.md (53 lines) + Step 6 commenting rules + Steps A–F including Step D1 surgical discipline + WRONG/RIGHT worked examples (all verbatim from SKILL.md) + 5-line substrate note (the only departure from CPM verbatim, replacing Step F's MCP-tool reference) + user instructions with Red-Zone pre-authorisation + output schema. MiniMax returned clean ```json-fenced edit list (11 lines); single fence-strip parsed; `ModifyText` applied via Adeu `process_batch` with no `BatchValidationError`. **Outcome C — pattern does not transfer down-tier.** One edit, `target_text`=29 words, `new_text`=56 words; `w:ins`=56 words OVER-BROAD, `w:del`=29 words SUSPICIOUS; five LCIA elements present; governing-law intact; audit trail clean; zero comments emitted (CPM commenting rule transferred). CPM's Step D1 surgical discipline + worked examples + edit-list contract + persona + authority framework, all ported verbatim, produced the same bundled-edit shape 10G / 10I-Sonnet / 10J produced under simpler prompts. Two non-exclusive readings: (1) CPM's pattern depends on frontier (Opus) decomposition judgement; explicit surgical language is scaffolding that helps but isn't load-bearing on weaker models; (2) the §9 litigation→arbitration transformation is structurally harder than CPM's confidentiality-clause worked examples; transformation-specific examples may be needed. New cross-sprint data point: **prompt disciplines transfer asymmetrically** — CPM's commenting rule transferred cleanly (`comment: null`); Step D1 span-size rule did not. Red-Zone pre-authorisation worked silently (MiniMax did not acknowledge the classification step; whether Step C was consulted at reasoning layer unknown under one-invocation budget). 10L chooses between (b) adding a transformation-specific RIGHT example, (c) re-running the same prompt on a frontier model via env-var flip, (d) accepting frontier capability as required for this stage — Arturs's call. No new ADRs, no new dependencies, no policy widenings, no `.env.example` changes.
- **Sprint 10L [Redline] (2026-04-21) — Port CPM's document-vs-new diff mechanism; re-process 10K's output.** Outcome C — feature-branch only per unsuccessful-sprint rule; sprint log entry on main. Phase 1 research note on main at `docs/redline/research/sprint-10L-port-feasibility.md` confirms `find_match_three_layer` + `diff_words` are liftable against Adeu 1.1.0 (public-attribute-only dependencies on engine; pure Python + `diff_match_patch` respectively), and records a manual trace showing Adeu's `trim_common_context` DID fire in 10K's run and returned `(0, 0)` (no shared prefix/suffix between target `"The parties submit..."` and new `"Any dispute or claim..."`). Port was therefore required, not side-steppable by re-invoking Adeu. `src/redline/experiments/sprint-10L/` with `post_processor.py` (ported `find_match_three_layer` + `diff_words` + `PlainTextIndex` + block-grouping at long-EQUAL boundaries + anchor-widening) and `run.py` (driver + 10E's verify_output inline + clean-view §9 read-back + transcript). 10K's `parsed-edits.json` and `nda-input.docx` copied via `git show` from the 10K feature branch; no branch checkout needed. Single run. **Outcome C — the ported mechanism produces byte-identical OOXML shape to 10K's direct-Adeu output: 1 block, w:del=29 words, w:ins=56 words, same single-edit wide bundle.** Diagnosis: `diff_words`'s post-diff `diff_cleanupSemantic` pass (CPM's choice, ported verbatim) collapses the scattered short shared runs between 10K's target and new_text into one wide DELETE + one wide INSERT. Raw `diff_main` without cleanup finds 72 tiny ops (including the shared " arising out of or in connection with this Agreement"); `diff_cleanupSemantic` reduces them to 2 as its semantic heuristic judges short-runs-surrounded-by-changes to be worth absorbing. CPM's narrow OOXML on Opus therefore depends on Opus producing `new_text` with longer preserved phrasing runs than MiniMax's wholesale rewrites — a behavioural dependency CPM's SKILL.md does not disclose. **The "missing post-processor" hypothesis is falsified for this transformation.** 10M proposal, keyed to the cleanup-pass diagnosis: (a) conservative-drafting prompting on MiniMax (restore the 10J-dropped preservation sentence; cheapest test of upstream-drafting-style lever); (b) same 10K prompt on Sonnet / GPT-5.4 via env-var flip (distinguishes tier-capability from prompt-scaffolding); (c) cleanup-pass probe — swap `diff_cleanupSemantic` for `diff_cleanupEfficiency` or no-cleanup-with-filtering (isolates the cleanup contribution from drafting); (d) accept frontier + mechanism as the production shape. Recommendation: (a) → (c) → (b) → (d). Anchor-widening logic untriggered on 10K's single-edit input (raw target was unique on first check; ready for future multi-block inputs). No new ADRs, no new dependencies, no policy widenings, no `.env.example` changes, no env-var triples changed.
- **Sprint M1 [Infrastructure] (2026-04-21) — Multi-track discipline infrastructure.** First M-series sprint. Track-aware `src/` layout (`src/shared/`, `src/redline/`, `src/cosec/`), track-aware `docs/` layout (`docs/redline/`, `docs/cosec/`; cross-track `docs/adr/` and `docs/reference/` preserved), new `CLAUDE.md § Multi-Track Discipline` section, retroactive `[Redline]` tags on all existing SPRINT_LOG headings and Sprint Index rows, `[Redline]` / `[Infrastructure]` track tags across all TODO.md items, ADR numbers 019-021 reserved as placeholder files for the redline track's 10H-deferred ADRs. No feature work, no agent code, no new dependencies, no policy widenings, no `.env.example` changes. Commits on main (infrastructure belongs to both tracks). Both tracks pick up the new structure on next pull.
- **Sprint 10M [Redline] (2026-04-21) — Port Vibe Legal Redliner verbatim onto Adeu 1.1.0; test MiniMax and Gemini Flash on Vibe's prompt + one-call pipeline.** Feature-branch only per unsuccessful-sprint rule; sprint log entry on main. Phase 1 research note on main at `docs/redline/research/sprint-10M-vibe-port-adaptations.md` (verbatim Vibe source extracts + Adeu 0.6.7→1.1.0 symbol-by-symbol compatibility check per CLAUDE.md §"Cross-Version Porting Research"). Port is one-import + five-constructor translation (`DocumentEdit → ModifyText`) in Vibe's `pipeline.py`; everything else passes through. Two runs: MiniMax-M2.7 and Gemini 2.5 Flash via OpenRouter (same NDA, same single-rule playbook, same pipeline, env-triple swap between runs). **Outcome C with a novel diagnostic variant.** **MiniMax:** 1 edit, `w:del=29`, `w:ins=54` (OVER-BROAD) split across 2 new paragraphs via `_insert_new_paragraphs`; all five LCIA elements present; **MiniMax applied Vibe's MISALIGNMENT-RIGHT anchor-preservation idiom for the first time in the arc** — `new_text` begins with verbatim governing-law prefix of `target_text`, so the word-diff narrowed the `w:del` to the jurisdiction sentence only and kept the governing-law sentence in-place. **Gemini:** 6 edits (1 MISALIGNMENT + 5 GAP), 1 applied and 5 skipped due to **novel chained-GAP failure mode** — each GAP's `target_text` references the PRIOR edit's `new_text`, which Vibe's pipeline applies independently against the original document, so all chained GAPs fail to match; clean-view §9 missing 4 of 5 LCIA elements. Both runs' JSON parsed at Layer 1 (`direct`); four-layer fallback untested in-sprint. Playbook-format deviation (target prose vs client-position statements) and styler-skip (`polish_formatting=False`) documented as intentional test-condition departures. Cross-sprint data points: (a) **MISALIGNMENT-RIGHT anchor-preservation DOES transfer down-tier to MiniMax** — the 10F–10L wholesale-rewrite behaviour was pattern-specific; a concrete sentence-level RIGHT example in the system prompt shifts MiniMax's `new_text` shape (positive); (b) **Gemini's chained-GAP strategy is a new prompt/pipeline hazard** — Vibe's prompt does not forbid chaining and Vibe's production playbook format side-steps it by distributing GAPs across distinct anchors; Oscar playbooks must either explicitly forbid chaining or use a flat rule format; (c) **playbook bullet-as-element vs bullet-as-rule is under-specified** — MiniMax read one rule with seven elements (1 edit), Gemini read seven distinct rules (7 analysis entries, 6 edits). 10N proposal: (a) frontier-tier on Vibe's prompt (Opus/GPT-5.4, one env-var flip — cheapest); (b) transformation-specific §9-forum-swap RIGHT example added to Vibe's prompt (extends 10K's (b) proposal with 10M's anchor-preservation transfer data); (c) flat 7-rule playbook on MiniMax (isolates playbook-format contribution from model-tier); (d) accept frontier-tier as required. Recommendation: (a) first, (c) second, (b) third, (d) landing. No new ADRs, no new dependencies, no policy widenings, no `.env.example` changes. `OSCAR_LLM_REDLINE_EXECUTOR_*` triple swapped at shell level between runs only.
- **Sprint 10N [Redline] (2026-04-26) — Replace the malformed eleven-sprint test (10F–10M, all on a forced single-clause litigation→arbitration on §9, where §9's flat structure has no surgical-edit surface area) with a representative test: a real solicitor's brief asking for multi-clause review on the same Acme NDA, MiniMax-only, single-shot. Adeu 1.1.0 → 1.3.3 upgrade.** Phase 1 research note at `docs/redline/research/sprint-10N-adeu-upgrade-and-prompt-comparison.md` (Adeu API surface verification + B1/B2 prompt comparison findings). Adeu upgrade preserves every API surface 10M's pipeline depends on — no breaking changes; `requirements.txt` bumped from `adeu==1.1.0` to `adeu==1.3.3`. Phase 1B compared **B1** (10M's Vibe system prompt minus the `## Output Format` schema and `### edit_type Values` sections; persona + structured-reasoning + Edit Precision Rules + WRONG/RIGHT examples preserved) against **B2** (the four-sentence solicitor system prompt verbatim from the brief) on the new user message (solicitor brief + NDA + data contract note). Both runs returned 4 edits with near-identical clause coverage; **headline difference is shape** — B1 produces surgically anchored sentence-level targets (Vibe's MISALIGNMENT-RIGHT discipline visibly fired), B2 produces wholesale clause replacements with heading-anchored targets and exhibits explicit scope creep ("also tightened wording…"). **Both runs and the Phase 3 run missed the mutual-obligations-throughout instruction** — substantive content gap that surfaces on Arturs's review. **Phase 3** ran B1 end-to-end, applied 4 edits / 0 skipped via `pipeline.apply_edits`, output `nda-output.docx` 39,318 bytes; mechanical verify_output passes (valid zip, parses, w:ins=6, w:del=5, litigation phrase preserved in w:delText). **Outcome A (mechanical) / Substantive verdict pending Arturs's review** of `nda-output.docx` on feature branch. Cross-sprint data points: (a) **the data contract note in the user message dominated over Vibe's structured-reasoning instruction in B1** — MiniMax dropped the `reasoning` object entirely both times; Vibe's classification-framework value is in the WRONG/RIGHT examples and Edit Precision Rules, not in the structured-reasoning + classification scaffold (when the data contract is `{"changes": [...]}`); (b) **the eleven-sprint decomposition-ceiling arc is closed by Arturs's reframing, not by 10N** — "MiniMax cannot produce narrow surgical-span redlines on §9" stands as a finding about a structurally-flat input clause, not about MiniMax's general capability; (c) **inline-path Markdown stripping persists from 10M as a known constraint** — `pipeline.py:_strip_formatting_markers` strips `**`/`_` before the inline word-diff path; Adeu 1.3.x's native markdown handling only fires on the delegation path. Phase 3 LLM did not emit Markdown markers, so the constraint did not manifest visibly; if Arturs's review notes lost formatting, lineage traces to 10M's inline-vs-delegation choice — 10O addresses if material. No new ADRs, no policy widenings, no new dependencies (Adeu version pin only).
- **Sprint 10O [Redline] (2026-04-26) — Planner-executor head-to-head against 10N (GPT-5.5 non-Pro plans + MiniMax executes, same NDA + brief).** Two-hypothesis test: (a) does the planner-executor split close the three 10N substantive gaps (clause 3 "who have a genuine need to know it" qualifier; clause 7 "Save for liability which cannot be limited or excluded by applicable law" catch-out; cross-clause "mutual obligations throughout"); (b) is GPT-5.5 non-Pro sufficient for the planner role (10P-target capability-ceiling check is GPT-5.5 Pro at $30/$180). Phase 0 confirmed Deep Agents 0.5.3 supports per-sub-agent model assignment but not sequential-mandatory primitive — direct Python pipeline chosen, no Deep Agents involvement. Phase 3 single run via OSCAR_LLM_REDLINE_PLANNER_* (openai/gpt-5.5) + OSCAR_LLM_REDLINE_EXECUTOR_* (MiniMax-M2.7). **Outcome A (mechanical) / Substantive verdict: production-acceptable, materially better than 10N — three of four 10N gaps closed (§3 need-to-know qualifier preserved with partner-quality group-companies definition; §7 mandatory-law catch-out preserved alongside fraud / wilful misconduct / confidentiality breach carve-outs in parallel rather than substitution; §4 connector renumbered cleanly with new (d) independent-development limb). One gap persisted: cross-clause "mutual obligations throughout" not propagated to recitals or asymmetric Purpose definition — GPT-5.5 non-Pro didn't recognise the brief instruction as implying multi-location edits. Two-hypothesis verdict: (a) architecture closes per-clause preservation failures — confirmed; (b) GPT-5.5 non-Pro sufficient for cross-document directives — failed.** Planner emitted 4 instructions + 4 cross_clause_notes, parse_method=direct; 4 executor calls all parsed at layer 1; 4 edits applied / 0 skipped via pipeline.apply_edits; nda-output.docx 39,363 bytes, valid zip, w:ins=6, w:del=5. Comments still don't land in .docx (Q1 caveat — inline-path bypasses Adeu delegation; full integration is 10P+ scope). pipeline.py docstring updated with Adeu upgrade-brittleness warning naming every private surface the inline path imports — flagged for 10P+ refactor onto process_batch (also resolves comment-attachment via inline path). Q1 comment-fix applied at run.py edits_for_pipeline boundary. .env.example documents both PLANNER and EXECUTOR triples for the experiment seam. Forward pointer: 10P-prep tests architecture portability when the client side flips (Oscar acts for Zenith); 10P proper tests counterparty response using 10P-prep's output; the cross-clause directive miss may become 10Q's tier-bump test. No new ADRs, no new dependencies, no policy widenings.
- **Sprint 10P-prep [Redline] (2026-04-26) — Oscar acts for Zenith, first-pass redline on the same Acme NDA (counterparty fixture for 10P).** Two-hypothesis frame: (a) does the 10O pipeline produce mechanically valid lawyer-shape output when the client side flips (architecture portability)? (b) does GPT-5.5 non-Pro produce supplier-side judgement under a deliberately thin five-sentence brief naming zero tactical positions? Phase 1A surface check confirmed Adeu 1.3.3's `RedlineEngine.author` propagates end-to-end through `_create_track_change_tag(author or self.author)` (engine.py:131-134) to every w:ins/w:del — single-site change at construction is sufficient. Phase 1B: branch `sprint-10P-prep-zenith-firstpass`; build_input/doc_analyser/prompts/parser copied verbatim from 10O (build_input md5 confirmed against 10E); user_prompt.txt = Zenith's verbatim approved brief; pipeline.py author swap on four sites; run.py author swap at prepare() call-site + `[10O]`→`[10P-prep]` log prefix replace_all; LLM-bound prompt files untouched. Phase 3 single run: planner emitted **7 instructions + 4 cross_clause_notes** (cf. 10O's 4 + 4) parse_method=direct; **7 executor calls all layer-1 parsed; 7/7 edits applied via pipeline.apply_edits**; nda-output.docx 39,873 bytes; w:ins=12, w:del=6; **100% Zenith Counsel attribution verified in raw OOXML (12/12 + 6/6)**; python-docx round-trip clean; planner `response_metadata.model_name=openai/gpt-5.5-20260423` per CLAUDE.md routing rule; metadata captured for planner + 7 executors. **Outcome A (mechanical) / Substantive verdict pending Arturs's review.** Hypothesis (a) — confirmed at mechanical layer; the pipeline is not coupled to Acme-side framing. Hypothesis (b) — pending substantive review: three of seven instructions cover supplier concerns the brief did NOT name explicitly (p4 independent-development carve-out; p5 return/destruction operational carve-outs for backups + compliance + internal records; p7 NEW clause 8A "No Warranty or Obligation to Proceed" — gap-fill insertion routed through Adeu's delegation path for the multi-paragraph case). Edit count 7 vs 10O's 4 against a brief that explicitly said "Don't over-mark" is an Arturs-judgement read between (i) over-marking and (ii) supplier-side review legitimately requiring more breadth. **Independent re-observation of 10O's cross-clause ceiling**: cross_clause_notes Note 1 recognised the Purpose↔Clause-2 cross-coupling in reasoning (*"broadening the Purpose is preferable to editing Clause 2"*) but did NOT emit a per-location Purpose-broadening instruction — same shape as 10O's mutual-obligations gap, second data point on the GPT-5.5 non-Pro cross-document propagation ceiling. The .docx becomes 10P proper's input — a tracked-change document with 18 changes attributed to "Zenith Counsel" that the counterparty-response workflow can route via state-of-play extraction (Phase 0 + 0.5 research). No new ADRs, no new dependencies, no policy widenings, no `.env.example` changes, no env-var triples changed.
- **Sprint 10P [Redline] (2026-04-26) — Counterparty-response pipeline on cut-down 3-change Zenith fixture (Outcome B mechanical; substantive verdict TBD).** Phase 0 inventoried 14 MCP files / ~1,400 LoC across 3 surfaces (counter-propose, state-of-play, comment-attaching — the last a NEW mandatory port surface driven by behavioural rule 4); Phase 1 ported 18 modules into `src/redline/lib/` with 3-case integration test passing on synthetic input; Phase 2.1 built the planner-executor pipeline (planner emits `NegotiationDecision` list with rule-3 reject excluded; executor fires only for counter_propose; dispatcher applies in 3 stages on a single in-memory Document via Adeu native + ported helpers). Phase 2.2 cut-down via lxml surgery (Adeu RejectChange would have pair-swept §1 keeper). Phase 2.3 single run (planner=openrouter/openai/gpt-5.5-20260423, executor=minimax/MiniMax-M2.7, 55.4s elapsed; routing confirmed via response_metadata): planner emitted 1 accept + 2 counter_propose (no no_action — diverged from brief's expected 1/1/1 shape); executor produced 2 single-edit JSONs both parsed direct; dispatcher applied 1 accept-with-comment (Chg:3 §6 backup), 1 surgical layered counter-propose (Chg:2 §3 affiliates+contractors — pure-strike narrowing, Acme w:del fragments NESTED INSIDE Zenith's w:ins, no sibling Acme w:ins because no new Acme tokens), and SKIPPED 1 counter-propose (Chg:1 §1 deletion — executor produced byte-identical replacement, dispatcher's `is_text_identical` gate fired). nda-output-minimal.docx 40,973 bytes; w:ins=1, w:del=3; two authors visible; w:commentReference=2. **Outcome B (mechanical) / Substantive verdict: production-acceptable on the two decisions that landed; Arturs reviewed and confirmed.** Two-hypothesis verdict: (a) architecture handles counterparty response — confirmed at mechanical AND cognitive layer; (b) GPT-5.5 non-Pro sufficient at three-decision scale (full confirmation deferred to Phase 3's 18-change coherence test). Brief-prediction divergence (1 accept + 2 counter_propose vs anticipated 1/1/1) is a positive finding — planner exercised independent judgement producing defensible different distribution. Three OOXML shapes now demonstrably working: Phase 1 surgical-with-shared-prefix; Phase 1 wholesale wrap; Phase 2 pure-narrowing nested-strike (new — Acme strikes inside Zenith's w:ins with NO sibling Acme w:ins because new_text introduces no new tokens). Comments land in OOXML (10N/10O caveat resolved via Stage C). Acme comments partner-quality drafting, not placeholders. **Phase 3.0 (commit `67c48c2` to main): gate fix applied per Arturs's selection of Option 1** — `counter_propose_inplace.is_text_identical` gate now permits byte-identical replacement specifically on deletion change_type, enabling the rule-3 escape "restore original Acme text" intent to produce a layered shape (Zenith w:del + Acme w:ins of identical text adjacent). Smoke-tested on Chg:1 with stub byte-identical executor: status=success, method=wholesale; restore shape produced as designed. **Phase 3.1 — full 18-change run, Outcome A mechanical (substantive verdict TBD).** Single LLM attempt at 128.7s (planner gpt-5.5-20260423; executor MiniMax-M2.7); planner emitted 18 decisions + 2 cross_clause_notes parse_method=direct, 1:1 with state-of-play; distribution 16 accept + 2 counter_propose (Chg:7 §3 affiliates broadening + Chg:16 §7 consequential-loss exclusion); both counters surgical method; nda-output.docx 43,033 bytes; w:ins=4 (2 Zenith + 2 Acme), w:del=3 (Acme nested fragments), w:commentReference=18 (all 18 comments anchored). Hypothesis (a) confirmed at 18-change scale; hypothesis (b) coherent — all 18 decisions reference real Chg:N IDs, internally consistent (§3 conforming Chg:8+9 accepted because Clause 7 cross-reference being countered separately on Chg:16), cross_clause_notes meaningfully populated (Note 2 connects Chg:7↔Chg:16), comments partner-quality across all 18. **Cross-run observation:** Chg:1 decision drifted between 2.3 (counter-propose to defend broad definition) and 3.1 (accept reasonable-understanding narrowing) — at 18-change scale the planner picks fights more selectively; gate-fix's real-LLM behaviour not directly retested in 3.1 (smoke-test verification stands). **Mechanical fixes during 3.1** (within the dispatcher-routing-fix allowance, single LLM call preserved via `--cache`): Stage C anchor logic switched to a fixed 80-char paragraph prefix after first-try `replace(d_text, "", 1)` proved fragile on short common deletions ("Party") that appear earlier in original text; recovered all 13 first-try comment-anchor failures. The prefix mechanism is robust across all observed paragraph shapes; not provably correct for paragraphs whose first 80 chars are NOT pre-edit-zone (open carry-forward). **Substantive verdict: production-acceptable.** Comment quality holds partner-shape across all 18 decisions (no placeholders); cross-clause reasoning coherent (§3↔§7 paired-decision chain); distribution calibrated (16 accept / 2 counter on the substantively important §3 disclosure perimeter + §7 confidentiality-breach liability). Chg:1 drift across 2.3 and 3.1 is a calibration finding (non-determinism on borderline calls — production should expect this; senior solicitor review remains load-bearing). Sprint 10P closes the redline track at the capability layer; production-readying work (RBAC, audit traceability, bias testing, live deployment) is separate. **Next sprint moves to the billing track** per Arturs's direction. Two new CLAUDE.md standing rules banked over the sprint (Phase-0 re-derivation; MCP dual-ID pattern). Five mechanical prompt fixes applied in Arturs's review pass (no behavioural change). Re-observed: 10C pair-sweep on RejectChange (worked around in fixture creation, not pipeline); 10L `diff_cleanupSemantic` short-shared-run collapse (smoke-test only; real-executor run produced surgical-narrow shape). No new ADRs, no new dependencies, no policy widenings, no `.env.example` changes.
- **Sprint M2 [Infrastructure] (2026-04-26) — Channel framework + Slack integration (AgentMail deferred).** Channel-Protocol-and-dispatcher framework spine: `src/shared/channels/{base,fake}.py`, `src/shared/dispatcher.py`, `src/shared/agents/general_counsel.py` (Sprint 9 GC pattern promoted verbatim with `MemorySaver` wired at build time), `src/shared/runtime/main.py` (long-running entry point with secrets-loader-first ordering, factory-injection seam for tests, SIGTERM/SIGINT graceful stop under a 25s timeout sized below Kubernetes `terminationGracePeriodSeconds: 30`). Two channels under `src/shared/channels/`: Slack (slack-bolt 1.27.0 Socket Mode, `app_mention` only, `conversation_id = f"{channel}:{thread_ts or ts}"`) and AgentMail (agentmail 0.0.50 SDK, WSS to `ws.agentmail.to/v0` + REST to `api.agentmail.to`, 5s reconnect backoff). Secrets pattern shifted from Sprint 3's in-sandbox `.env` to `/etc/oscar/oscar.env` on the host, materialised as Kubernetes Secret `oscar-runtime-secrets` and mounted on the Sandbox CR's pod template at `/etc/oscar` read-only — `src/shared/secrets.py:load_host_secrets()` reads it at runtime startup; LLM credentials migrated from `/sandbox/oscar-enterprise/.env` to the host file at end of Phase 2. Sandbox CR manifest committed at `deploy/kube/oscar-sandbox.yaml` (portable template — per-instance fields injected by the OpenShell controller on fresh-create, or mirrored explicitly via downward API + `secretKeyRef` on `kubectl apply`). 53 unit tests passing (23 Slack + 12 AgentMail + 5 dispatcher + 5 runtime + 8 secrets), pure-Python with `FakeChannel` and stub factories. ADRs 023 (Channel Protocol and dispatcher), 024 (Channel deployment topologies — Slack Socket Mode + AgentMail WebSocket), 025 (Secrets on host, mounted as a Kubernetes Secret-volume into the sandbox; with a Lessons learned section added at sprint close capturing 16 architectural findings from the Phase 2 recovery cascade). **Live integration test passed:** user @-mentioned `@Oscar GC are you there?` in `#oscar-test`; SlackChannel received the `app_mention` over Socket Mode, dispatcher routed via the GC chain, GC posted **`Yes.`** as a threaded reply. Follow-up `@Oscar GC What did I just ask you?` in the same thread produced **`You asked: "are you there?"`** — confirming `MemorySaver` preserves conversation state and the dispatcher's `thread_id := conversation_id` derivation maps same-thread turns onto the same LangGraph state. **AgentMail deferred to a separate post-M2 sprint:** channel implementation, policy egress block, and Secret-loading machinery all in place and unit-tested but inactive in the runtime — `runtime/main.py` ships with the AgentMail factory commented out behind a `TODO` because `OSCAR_AGENTMAIL_API_KEY` and `OSCAR_AGENTMAIL_INBOX_ID` are not yet provisioned in `/etc/oscar/oscar.env`; activation is one block-uncomment + factory plumbing-in once Arturs provisions credentials. **Phase 2 Recovery (~3 hours):** `kubectl apply` of the Sandbox CR manifest triggered cascading "mechanism reports success but behaviour differs" issues against OpenShell v0.0.32 — required env vars (`OPENSHELL_SANDBOX_ID`, `OPENSHELL_SSH_HANDSHAKE_SECRET`) had to be re-added via downward API + `secretKeyRef` after stripping for portability; the downward API resolution failed because pods don't inherit CR labels (fix: mirror `openshell.ai/sandbox-id` onto `spec.podTemplate.metadata.labels`); the OPA proxy denied Slack/AgentMail traffic even after policy redeploy because the canonical Python path wasn't in the `binaries:` list — symlink resolution via `/proc/<pid>/exe` matches against the canonical target, not the symlink path (fix: mirror the established `minimax` block with a `/sandbox/.uv/python/**` covering glob, commit `2dc1a6b`). Sixteen findings recorded in ADR 025 § Lessons learned across OpenShell controller behaviour, gateway/policy behaviour, policy file conventions, operational discipline, and Slack-app provisioning. New dependencies `slack-bolt==1.27.0`, `agentmail==0.0.50`, `pytest`, `pytest-asyncio`. Policy widenings: `slack` and `agentmail` egress blocks; `/etc/oscar/oscar.env` as explicit-intent Landlock entry. `.env.example` adds four placeholder triples. Coordination with redline (`sprint-10P`, `sprint-10Q`) and CoSec (`sprint-c1`) tracks unimpeded throughout — `git pull --rebase origin main` discipline absorbed concurrent commits cleanly.

